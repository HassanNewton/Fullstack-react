import LoginForm from "../components/LoginForm";

const LoginPage = () => {
  return (
    <div className="login-page">
      <h1>Logga in</h1>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
